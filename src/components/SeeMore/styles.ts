import styled from "styled-components/native";

export const ShowMoreOrLessText = styled.Text`
  font-family: "Poppins_600SemiBold";
  font-size: 13px;
  margin-top: 0px;
  color: ${({ theme }) => theme.COLORS.TEXT};
`;
